import { Wrench } from "lucide-react"
import { PlaceholderPage } from "@/components/placeholder-page"

export default function PitScoutingPage() {
  return (
    <PlaceholderPage
      title="Pit Scouting"
      description="This will hold the pit scouting form scouts fill out for each assigned team before matches start."
      icon={Wrench}
    />
  )
}
