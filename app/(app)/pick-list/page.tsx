import { Kanban } from "lucide-react"
import { PlaceholderPage } from "@/components/placeholder-page"

export default function PickListPage() {
  return (
    <PlaceholderPage
      title="Pick List"
      description="This will hold the drag-and-drop alliance pick list plus the alliance selection simulator, built from scouted match and pit data."
      icon={Kanban}
    />
  )
}
